package com.example.pampiway.utility

import androidx.compose.ui.text.font.Font
import androidx.compose.ui.text.font.FontFamily
import com.example.pampiway.R

val firaSans_regular = FontFamily(
    Font(R.font.firasans_regular)
)

val firasans_medium = FontFamily(
    Font(R.font.firasans_medium)
)

val firasans_bold = FontFamily(
    Font(R.font.firasans_bold)
)

val robot_regular = FontFamily(
    Font(R.font.roboto_regular)
)

val robot_bold = FontFamily(
    Font(R.font.roboto_bold)
)